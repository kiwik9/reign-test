package io.kiwik.reign.repository

import io.kiwik.reign.domain.entities.*

class DataRepository : BaseRepository() {

   // suspend fun insertCards(vararg cards: Card) = dataDao.insertCards(*cards)

}